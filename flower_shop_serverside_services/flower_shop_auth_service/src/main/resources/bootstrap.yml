server:
  port: 8081

spring:
  application:
    name: auth-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
      fail-fast: true
      password: pass
      username: user