server:
  port: 8085

spring:
  main:
    allow-bean-definition-overriding: true

  application:
    name: florist-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
      fail-fast: true
      password: pass
      username: user